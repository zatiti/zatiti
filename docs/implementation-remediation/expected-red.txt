# Packages whose `go test` currently fails for a KNOWN, TRACKED reason during
# the implementation-remediation rollout (docs/implementation-remediation/),
# not a fresh regression. One import-path suffix (relative to
# github.com/zatiti/zatiti/) per line, blank lines and #-comments ignored.
#
# hooks/pre-commit treats a package listed here as allowed to fail; a
# failing package NOT listed here still blocks the commit. Remove a line the
# moment its card lands and the package goes green again -- do not leave a
# stale entry once it's actually fixed, and do not add a package here for
# any reason other than "P00 froze a contract operation this package's Go
# code doesn't implement yet." See docs/lore.md, "Expected red:
# catalog/descriptor-parity tests" for the full reasoning and which card
# fixes each one.
#
# Founder decision (David, 2026-09-19, in-session): merge P00's contract
# freeze ahead of its implementations and track the red via this allowlist,
# rather than batching cards before touching main or using a separate
# integration branch.
#
# Founder decision (David, 2026-09-20, in-session): P37 (internal/registry)
# regenerated catalog.json to the correct revision-3 shape of the shared
# $defs (Operation/Artifact/Responsibility/Conversation), which is P37's own
# job and is correct -- but every module still shipping the old shape now
# fails registry assembly ("operation schema redefines the shared
# definition X"), which is the entries below. P09 (internal/artifacts) is
# structurally blocked from landing until this entry set is added, since it
# embeds Artifact/Operation directly -- there is no order that avoids this.
# Remove each line the moment P06/P07/P09/P10 land and the assembly-wide
# check passes clean again; see docs/roadmap.md's 2026-09-20 entry.

cmd/zatiti
internal/application
internal/installation
internal/scheduling
internal/skills
tests/integration
tests/qualification
