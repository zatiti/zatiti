# P03 — Bind worker principals and expose authenticated caller metadata

Status: planned. Priority: P0. Owner: `internal/identity`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/identity`.

Implement in this order:

1. Add identity.current from the authenticated actor, exposing principal kind/scope only, never a credential.
2. Implement the internal worker-subject resolver from P00: a controller can resolve a configured worker to its own scoped principal, but cannot choose an arbitrary human identity.
3. Ensure personal chiefs, child chiefs and ordinary workers can be provisioned atomically by authorized configuration/bootstrap calls with stable worker-principal bindings.
4. Recheck revocation on every resolution and dispatch; preserve service-principal privileges only for controller bookkeeping.

Required behavioral tests:

- A worker cannot obtain the owner identity or another worker's scope.
- Revocation between context construction and dispatch denies the action.
- Bootstrap/replay creates one chief principal; identity.current agrees over CLI, MCP and desktop.

Package check: `go test ./internal/identity`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Worker identity and caller-discovery operations with revocation tests. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
