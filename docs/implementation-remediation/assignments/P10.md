# P10 — Add durable intent delivery and complete chat queries

Status: planned. Priority: P0. Owner: `internal/messaging`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/messaging`.

Implement in this order:

1. Add controller/execution-facing ready-recipient discovery and atomic turn admission/ack fences using message ID plus recipient ID as the deduplication identity.
2. Do not acknowledge a worker message until an execution turn or safe-boundary injection is committed in the same unit; reading a mailbox is not processing.
3. Implement authorized conversation.message.list with stable paging, sender-inclusive history, current participant checks and the declared history-disclosure boundary for newly joined participants.
4. Expose preview/unread/read-marker projections and exact meaningful-activity ordering; routine agent coordination must not reorder human chats.
5. Support durable worker replies/task links/group attachments and cancellation or clarification correlation without parsing free text into authority.

Required behavioral tests:

- Lost ack/restart admits one turn and one reply; multiple recipients each get their own delivery.
- Reopened desktop sees both previously sent and received messages without relying on window memory.
- A newly joined participant cannot read earlier undisclosed history, and quiet worker chatter leaves unread/order unchanged.

Package check: `go test ./internal/messaging`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Durable message-to-turn inbox seam and complete conversation read surface. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
