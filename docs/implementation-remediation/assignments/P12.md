# P12 — Finish effect reconciliation and durable callback ownership

Status: planned. Priority: P0. Owner: `internal/effects`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/effects`.

Implement in this order:

1. Persist an explicit callback route and dispatch mode outside adapter Action parameters; validate source/owner linkage so arbitrary JSON cannot select an execution callback.
2. Replace operation.reconcile's stalled job with a separately admitted bounded read linked to the original logical effect, exact prior attempt/provider reference and review/policy constraints.
3. Return a dispatch mode of reconcile only for the admitted lookup; controller must call Adapter.Reconcile, never Invoke on original mutation bytes.
4. Record delayed/contradictory evidence into the original effect without allowing later failure to erase earlier uncertainty or established success.
5. Use fair pending scans/cursors so unresolved operations cannot permanently hide new dispatchable work beyond the batch limit.

Required behavioral tests:

- Lost write response followed by reconcile produces one write and only authorized read calls; never a second write.
- A reconciliation job reaches a real terminal/unknown disposition and duplicate callbacks do not settle twice.
- More than 100 blocked operations do not starve a later ready operation.

Package check: `go test ./internal/effects`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Executable reconciliation and explicit callback routing with crash tests. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
