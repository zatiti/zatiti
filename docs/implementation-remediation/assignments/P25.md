# P25 — Make first-use guidance reach actual useful work

Status: planned. Priority: P0. Owner: `internal/installation`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P24, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/installation`.

Implement in this order:

1. Replace the current draft-only FirstTaskSequence with a truthful sequence for configure provider/budget/tool/worker, create/start task and inspect verified output.
2. Keep zero-spend initialization separate from paid work and expose missing setup requirements rather than requiring users to self-publish arbitrary verifier qualification evidence.
3. Provision trusted installed verifier profiles/evidence through the installation contract; capability evidence must reflect a real supported verifier build.
4. Report stalled turns, unclaimed jobs, unavailable memory/runner and outstanding liabilities in doctor/status.
5. Test first-task documentation as executable commands ending in a verified artifact, plus an unconfigured installation that remains honest and usable.

Required behavioral tests:

- Fresh installation can complete the documented controlled-fixture first task; terminal state is succeeded, not draft.
- Missing provider/currency yields named actionable requirements and no spend.
- Untrusted/self-published verifier evidence does not manufacture installed capability qualification.

Package check: `go test ./internal/installation`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: First-use diagnostics and a first-task recipe that does work. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
