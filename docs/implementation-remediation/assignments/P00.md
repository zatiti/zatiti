# P00 — Freeze the remediation contracts and regenerate assignments

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `tools/specgen`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: none. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `tools/specgen`, `docs/implementation/contracts.md`, `docs/implementation/requirements.json`, `docs/implementation/acceptance.json`, `docs/implementation/adapter-schemas.json`, `generated specification outputs (serialized renderer only)`.

This is a serialized shared/foundation assignment. No child-package writers may run while it changes shared files or generated prompts.

Implement in this order:

1. Read this audit, contract-proposals.md, the 127 requirement blocks and 116 acceptance cases. Turn the proposed seams into revision 3 authored schemas and contracts; do not weaken existing acceptance.
2. Reconcile the recorded revision-3 backlog in docs/roadmap.md against current code: registry version-0 lookup and self-contained schema normalization, CLI root tokens, public/internal caller declarations, service-principal bootstrap, artifact locator staging, review eligibility and internal authority checks. Preserve tested current behavior unless a coordinated migration explicitly changes it.
3. Freeze the Responses prepare_session/model_step split: two explicit effects with a persisted session handle, each making one physical call; specify disclosure, timeout, cost and unknown-outcome behavior for both.
4. Add durable worker turns, task start/readiness, worker-subject dispatch, context preparation/publication, named output slots, job ownership, reconciliation requests, verification scans, history and restore capabilities described in contract-proposals.md.
5. Update model.py, packages.py and authored inputs; enumerate every affected owner/caller and migration. Generate exact DTO examples for success, denial, duplicate, stale, unknown and cancellation paths.
6. Resolve model action routing using an explicit controller-owned route, never by inserting undeclared attempt_id into strict adapter parameters. Pin schemas for local proposals separately from provider actions.
7. Publish self-contained updated AGENTS.md assignments and golden JSON fixtures before any downstream writer starts. Run render and render --check; retain all old gates and add the new vertical journeys.

Required behavioral tests:

- Renderer passes without the RFC or network and generated files have no drift.
- Each internal operation has exact callers, transaction mode, input/output schema and replay semantics; invalid authority fields are rejected.
- All new fields have migration/default/version rules; old persisted tasks and operations remain inspectable.

Package check: `python3 tools/specgen/render.py --check (plus assignment-specific checks above)`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Revision 3 contracts, generated prompts, schema fixtures and caller matrix; serialized specification assignment. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
