# P29 — Add safe storage snapshot and offline restore mechanics

Status: planned. Priority: P1. Owner: `internal/storage`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/storage`.

Implement in this order:

1. Implement the narrow backup inventory/restore capability frozen in P00; expose migrations/schema checks through a typed capability, not arbitrary domain SQL access.
2. Validate an imported SQLite image and schema versions in staging before replacing the live file; checkpoint/close writers and prevent WAL mixing.
3. Provide crash-safe replacement/rollback markers and ownership/generation fencing for entrypoint/controller use while domain services are closed.
4. Support merge of the typed monotonic recovery overlay through owner migrations/ports after reopen; storage must not invent domain reconciliation.
5. Restore remains paused and cannot reopen the public mutation surface until verification/overlay completion.

Required behavioral tests:

- Crash at each staged-image/rename/reopen boundary yields a recoverable old or new database, never a mixed WAL.
- Wrong installation/schema/image digest refuses before replacement.
- Reopened database has a newer generation and exactly one writer.

Package check: `go test ./internal/storage`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Narrow real database rewind primitive and recovery journal. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
