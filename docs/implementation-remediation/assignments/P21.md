# P21 — Complete cooperative recovery and run export jobs

Status: planned. Priority: P1. Owner: `internal/execution`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P18, P02, P20. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/execution`.

Implement in this order:

1. Make cooperative claim return a published context artifact rather than the current digest-only synthetic envelope; show advisory posture and required capabilities.
2. Resume waiting runs only after unknown effects/leases are resolved under explicit recovery policy; enforce cumulative root/model-step/deadline limits across replacement attempts.
3. Implement run.export as a durable bounded job including accepted task, contexts, attempts, effects, outputs, costs and verifier evidence with source digests.
4. Provide terminal job callbacks and export artifact metadata; expose recovery obligations in task/run inspection.
5. Verify report outputs belong to the claimed worker/attempt and current lease.

Required behavioral tests:

- External worker claims, checkpoints, reports and receives independently verified result through supported public operations.
- Lost claim acknowledgement replays the same lease/context and stale heartbeat cannot revive it.
- run.export reaches succeeded and produces readable canonical history; restart preserves uncertainty.

Package check: `go test ./internal/execution`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Usable external-worker lifecycle and run history export. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
