# P46 — Prove vertical journeys and all-operation behavior

Status: planned. Priority: P0. Owner: `tests/integration`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P25, P18, P12, P17, P19, P21, P35, P37, P39, P40, P41, P02, P20, P28, P33, P36. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `tests/integration`.

Implement in this order:

1. Add a full real-owner fixture with the actual controller, Responses adapter pointed at a controlled provider, real artifact store and trusted verifier; fake only external services/time where specified.
2. Prove message-to-chief-to-task-to-tool-to-output-to-verification-to-reply with no test SQL writes or internal operations standing in for missing production code.
3. Expand current nine-step parity script to all public operations using legal prerequisite lifecycles, not merely descriptor discovery or invalid-input calls.
4. Run CLI subprocess and real MCP client variants, plus cross-interface continuation; normalize only generated IDs/timestamps explicitly.
5. Inject crash/revocation/pause/stale-version/funding/unknown-effect failures at every durable boundary and assert external physical call counts.
6. Extend restore/memory/engineering fixtures when P20/P28/P33 land; record these as explicit additional dependency gates before the full release matrix passes.

Required behavioral tests:

- Z03 and setup/research journeys finish with readable independently verified artifacts through both interfaces.
- Hosted tools, cooperative claims, skill evaluation, cycles, review and reconcile all finish using production paths.
- One-reply/one-effect invariants hold across every lost-ack/restart injection, and every operation has positive and negative behavior coverage.

Package check: `go test ./tests/integration`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: A real end-to-end suite exposing missing orchestration rather than hiding it behind fakes. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
