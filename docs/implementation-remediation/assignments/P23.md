# P23 — Attach verifier, local jobs and reconciliation driver

Status: planned. Priority: P0. Owner: `internal/controller`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P22, P18, P19, P21, P12, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/controller`.

Implement in this order:

1. Add declared verifier/job collaborators and execute them outside Units with bounded concurrency, deadlines and explicit owner callbacks.
2. Drive verification pending/claim/result records; retain independent verifier identity and publish staged request/verdict bytes before domain completion.
3. Register supported jobs by exact owner/operation; support skill evaluation, configuration/run exports and reconciliation. Keep unknown job kinds pending with a visible requirement.
4. For reconciliation consume the new admitted read dispatch and call Adapter.Reconcile once; route the observation back to the original effect owner and finish the job.
5. Repair post-claim crash recovery with a durable claim lookup rather than phaseStranded merely because the current pending scan cannot reveal a claimed job version.

Required behavioral tests:

- Every registered job kind reaches its actual completion through real owners.
- Crash after claim commit but before ack can inspect the exact claim and records unknown or safely resumes according to its mode.
- Reconcile call counter proves one original write and bounded lookup reads; verifier cannot be replaced by a worker runner.

Package check: `go test ./internal/controller`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Running durable jobs and independent verification in production. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
