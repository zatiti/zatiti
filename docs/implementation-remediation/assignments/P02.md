# P02 — Reconcile toolchain and dependency evidence

Status: planned. Priority: P0. Owner: `foundation`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P01. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `go.mod`, `go.sum`, `tools.go`, `docs/implementation/dependencies.lock.json`.

This is a serialized shared/foundation assignment. No child-package writers may run while it changes shared files or generated prompts.

Implement in this order:

1. Integration owner alone owns root go.mod/go.sum, tools.go if needed, and docs/implementation/dependencies.lock.json during this assignment.
2. Compare actual Go imports, pubspec.lock, Flutter/Dart metadata and adapter protocol documents with the lock; remove stale Fyne qualification claims and distinguish historical from current evidence.
3. Record exact existing pins and licenses. If a controlled repository runner or Serenity seam needs a new library, resolve and pin it here before downstream use; never silently download latest at runtime.
4. Keep unsupported provider hard caps and Serenity capabilities explicitly blocked. Record which controlled-fixture evidence and which real-service evidence are required.
5. Land this root change before child-package implementation begins; no root-writing lane runs concurrently with descendants.

Required behavioral tests:

- Module graph, Go version and Flutter lock agree with committed manifests.
- CI lock validation accepts this actual source tree and rejects a changed dependency hash.
- No provider capability becomes qualified merely by editing a lock entry.

Package check: `python3 tools/specgen/render.py --check (plus assignment-specific checks above)`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: An honest dependency and capability lock; exact build commands and known prerequisites. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
