# P11 — Make tasks startable and unblock dependency progress

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `internal/tasks`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/tasks`.

Implement in this order:

1. Implement versioned task.start as the explicit draft-to-ready public path using existing applyTransition, preserving draft creation semantics.
2. Recheck prerequisites, sealed acceptance, budgets, dependencies and worker binding at start; enqueue atomically and deduplicate the current accepted task version.
3. Replace preknown-output-digest dependence with sealed named-output checks from P00; only digest checks for known content require an expected digest at task admission.
4. Record and consume verified evidence through an explicit owner port, then wake dependent tasks only after required children independently succeed.
5. Handle reassignment or pending-input changes for already queued tasks: invalidate/supersede old queued runs and pin the new contract rather than executing stale work; preserve cancellation/retry fences.

Required behavioral tests:

- Public create then start creates exactly one ready run; duplicate start/key and concurrent start behave deterministically.
- A generated report whose bytes were unknown at admission can satisfy a sealed schema/presence check, but a worker assertion cannot.
- Failed child, stale accepted inputs, cancellation and changed assignment prevent old queued work from starting.

Package check: `go test ./internal/tasks`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Usable task lifecycle and generated-output acceptance without weakening verification. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
