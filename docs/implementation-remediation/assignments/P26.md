# P26 — Finish memory projections and governed context integration

Status: planned. Priority: P1. Owner: `internal/memory`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/memory`.

Implement in this order:

1. Add authorized memory.list/source/freshness/lineage projections for the desktop and context builder; do not invent claims when Serenity is unavailable.
2. Expose context selection as a governed durable job with exact brain revisions, scope, classification, freshness and disclosure/spend bounds.
3. Persist selected excerpts and provenance into immutable artifacts; use completion callbacks to finish owner/job state.
4. Implement promotion/retraction follow-up queue and corrected-source lineage so local metadata alone never claims the upstream write succeeded.
5. Keep unavailable upstream guarantees named, with read-only/advisory/enforced capabilities distinguished.

Required behavioral tests:

- Unauthorized brain is not queried; a revoked binding excludes future context but retained old context is unchanged.
- Stale/missing provenance produces explicit prerequisite rather than fabricated memory.
- Duplicate completion and restart preserve one selection/promotion lineage.

Package check: `go test ./internal/memory`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Complete local memory read/projection and governed job contracts. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
