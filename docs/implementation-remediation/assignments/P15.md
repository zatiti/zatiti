# P15 — Build and publish the actual model context

Status: planned. Priority: P0. Owner: `internal/execution`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P14, P06, P07, P09, P13, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/execution`.

Implement in this order:

1. Implement context prepare/perform/commit from P00. Prepare resolves pinned configuration/task/history/tool/memory references in a read transaction; perform reads bounded artifact bytes and stages outside the Unit; commit validates pins and publishes metadata/lineage.
2. Build zatiti.context/v1 in exact transcript order from worker instructions, immutable skill closure, accepted task, authorized inbox, selected memory, prior outputs and tool results. Mark untrusted parts by origin.
3. Carry only allowed tool schemas; empty bindings yield no tools. Enforce total token/byte limits, classification and destination constraints before dispatch.
4. Construct exact Responses action fields schema, kind, context_artifact, max_output_tokens, tool_contract_versions and optional continuation_reference. Resolve actual Tool and Connection refs/account; remove profile-as-tool and hardcoded connection version assumptions.
5. Advance context every turn/step; retained contexts remain immutable and readable after source memory/config changes.

Required behavioral tests:

- Produced context and action pass real adapter schemas, then a controlled provider sees the intended ordered messages.
- Missing instruction bytes, stale config or out-of-scope memory refuse before network IO.
- A tool result appears once in the next context; previous request context bytes never change.

Package check: `go test ./internal/execution`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Persisted model-ready context and schema-valid model effects. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
