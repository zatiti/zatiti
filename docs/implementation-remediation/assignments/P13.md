# P13 — Finish Responses-to-runtime integration contract

Status: planned. Priority: P0. Owner: `internal/adapters/responses`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/adapters/responses`.

Implement in this order:

1. Retain the existing adapter and protocol implementation. Validate runtime-created actions against zatiti.responses.action/v1 and expose fixtures using actual context artifacts/tool closure.
2. Split OpenAI conversation/session preparation from the model call into separately admitted, journaled single-call effects. Persist the session handle before the model call; retain unknown preparation outcome and never use an empty conversation as proof of nonexecution. P00 must add the exact prepare_session action and pinned session-reference schema.
3. Define normalization for text, tool_proposals, continuation, refusal, truncation and malformed output; never execute model proposals inside the adapter.
4. Ensure request-context and output artifacts can be published by controller and consumed under their normalized stable references.
5. Keep one physical call per dispatch, explicit price/cost/disclosure limits, no hidden SDK retries and unsupported guarantees visible.
6. Extend protocol fixture coverage for multi-turn tool results and the exact proposed runtime stop behavior.

Required behavioral tests:

- Action/context produced by execution is accepted by this real adapter without schema bypass.
- One model tool proposal returns inert typed data; malformed/foreign source-context proposals fail.
- Lost response, missing usage and truncation retain honest disposition and cost.
- Call counters observe at most one HTTP request per claimed effect; crash between prepare_session and model_step does not repeat either call, and reconciliation never invents missing usage.

Package check: `go test ./internal/adapters/responses`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Real adapter contract fixtures and output semantics for the worker loop. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
