# P38 — Carry new operation and event recovery semantics in the Go client

Status: planned. Priority: P1. Owner: `internal/client`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P37, P34, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/client`.

Implement in this order:

1. Support revision 3 version negotiation and new stable event cursors without duplicating domain logic.
2. Retain complete command lookup coordinates: scope, operation, version and original submission key; update regression fixtures for worker/task lifecycles.
3. Separate no-bytes-sent, unknown acknowledgement, accepted job and completed result states.
4. Expose bounded reconnect/poll helpers that do not silently resend mutations.

Required behavioral tests:

- Lost task.start or review decision acknowledgement resolves the original command, not a second mutation.
- Expired events resynchronize an authorized snapshot without replaying writes.
- Unsupported server version remains an actionable permanent error.

Package check: `go test ./internal/client`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Client support for the new lifecycle and recovery operations. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
