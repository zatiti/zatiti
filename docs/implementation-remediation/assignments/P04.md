# P04 — Route worker proposals through the common authorization boundary

Status: planned. Priority: P0. Owner: `internal/application`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P03, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/application`.

Implement in this order:

1. Implement WorkerOperator from the frozen contract as a controller-only capability; resolve worker actor through identity instead of accepting an actor JSON field.
2. For each proposal recheck current worker grants, bindings, installation restrictions, scope and operation allowlist before the ordinary application dispatcher runs.
3. Use deterministic submission identities derived from turn/proposal identity. Preserve original command IDs and result envelopes on replay, including refusals.
4. Support local IO outside transactions using existing Prepare/Perform/Finish; never nest network or model calls inside a database Unit.
5. Keep model-visible operations separate from controller internal methods and human decisions.

Required behavioral tests:

- A malicious proposal for grant.create, review.decide or an internal owner operation is denied unless the explicit worker-facing contract permits it; human-required decisions always refuse.
- Crash after proposal commit then redeliver: one domain mutation and same command result.
- Pause/revoke between proposal and admission is observed by the real dispatcher.

Package check: `go test ./internal/application`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: A narrow authenticated proposal executor, usable by hosted and cooperative paths. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
