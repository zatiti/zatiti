# P27 — Implement the missing qualified Serenity service seam

Status: planned. Priority: P1. Owner: `internal/adapters/serenity`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P26, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/adapters/serenity`.

Implement in this order:

1. Begin with PROTOCOL.md and current capability report: all actual operations currently refuse and no physical request is made.
2. Qualify a public pinned service/read facade against the frozen local memory schemas. Implement only the operations whose actual upstream guarantees meet freshness, scope, bounded cost and disclosure requirements.
3. Translate read/context results into staged artifacts with provenance and pinned brain/index revisions; keep unsupported writes disabled with exact gaps.
4. For writer mode require one writer owner, stable command IDs, lookup/reconciliation and actual aggregate cost enforcement; implementing these upstream is an external prerequisite, not permission to edit another repository.
5. Record reproducible protocol-fixture evidence and required real-service qualification; do not relabel the current capability-only refusal adapter as working memory.

Required behavioral tests:

- Qualified reads issue bounded requests and return real claims/provenance; unauthorized destination issues zero calls.
- Timeout/lost write ack remains unknown with no replay; unavailable required guarantee stays disabled.
- Backup/export/freshness behavior is exercised against the pinned service before writer mode is advertised.

Package check: `go test ./internal/adapters/serenity`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: A functioning qualified Serenity adapter or a precise upstream blocker per required capability. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
