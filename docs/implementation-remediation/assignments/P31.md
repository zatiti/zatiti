# P31 — Build complete backup bundles and monotonic recovery overlays

Status: planned. Priority: P1. Owner: `internal/installation`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P25, P29, P30, P09, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/installation`.

Implement in this order:

1. Replace empty manifest lists with actual schema versions, encrypted artifact bytes/pins, brain exports/revisions, adapter profiles and retained obligations, all from narrow owner ports.
2. Capture a consistent paused/quiesced snapshot; required unavailable brain/blob capability must block a complete backup rather than yield success.
3. Include current credential/grant revocations, dispatch claims, provider keys, unknown effects/costs and cancellations in the authenticated monotonic overlay with enough records to merge, not just a summary digest.
4. Validate source binding/version/key prerequisites and stage a controller-readable restore work item; expose verification and progress through installation.job.get.
5. Record completion only after actual database/blob rewind and overlay merge. Never auto-resume.

Required behavioral tests:

- Back up real artifact and domain state, restore elsewhere, read identical bytes and find the same accepted task/history.
- A post-backup revocation and unknown write remain effective after restore.
- Missing brain export or referenced blob blocks complete backup; partial backup cannot be advertised as complete.

Package check: `go test ./internal/installation`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Complete recoverable backups and explicit paused restore protocol. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
