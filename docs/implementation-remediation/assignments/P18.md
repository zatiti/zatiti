# P18 — Drive independent verification and bind real output evidence

Status: planned. Priority: P0. Owner: `internal/execution`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P16, P09, P11, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/execution`.

Implement in this order:

1. Expose pending/claim/request/record verification work separately from generic jobs, or implement the exact unified mapping frozen in P00; never leave execution_verification_jobs undiscoverable.
2. Resolve named output slots to published attempt artifacts and populate VerificationRequest.Outputs; seal the exact request digest before dispatch.
3. Invoke the trusted verifier outside all Units through the controller; record only results from that registered verifier identity/code digest, never a worker-supplied passed flag.
4. Publish request/verdict artifacts and hand verified evidence to tasks before its success transition. Distinguish failed checks, unavailable verifier, manual acceptance and interruption.
5. Make claim/record/restart idempotent and preserve unknown costs separately from a verification verdict.

Required behavioral tests:

- A reported task reaches succeeded via real NewVerifier and tasks evidence, without directly inserting SQL rows or calling record from a test.
- Missing bytes, wrong digest, tampered acceptance/profile or failed required child prevents success.
- Crash at verifier claim/result publication/record yields one verdict with the correct task/attempt binding.

Package check: `go test ./internal/execution`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: A complete report-to-independent-result path. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
