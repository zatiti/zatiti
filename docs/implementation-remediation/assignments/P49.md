# P49 — Reconcile documentation and close the delivery matrix

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `documentation`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P48, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `README.md`, `docs (authored status and operator documentation only)`.

This is a serialized shared/foundation assignment. No child-package writers may run while it changes shared files or generated prompts.

Implement in this order:

1. Integration owner updates authored README/status/setup/recovery docs with actual supported behavior and executable examples; regenerate spec index rather than editing generated README directly.
2. Remove stale claims that all code is absent, Responses has not landed, Fyne is the desktop, or fixed first-task/command-lookup defects still apply.
3. Preserve honest limitations for provider guarantees, memory, platform support and external-worker containment; no skipped test becomes qualification.
4. Record all package/operation/requirement/case statuses and exact evidence links. Any unresolved external prerequisite blocks only explicitly identified claims and remains visible.
5. Publish nothing externally as part of this repository documentation task.

Required behavioral tests:

- Documented first-use commands reach the promised terminal output in CI.
- All 36 scopes, 264 baseline operations plus additions, 127 requirements and 116 cases plus additions have owners and evidence status.
- No work item is marked complete solely because it compiles, has a plan, or has a skipped test.

Package check: `python3 tools/specgen/render.py --check (plus assignment-specific checks above)`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Accurate product documentation and closed implementation/evidence ledger. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
