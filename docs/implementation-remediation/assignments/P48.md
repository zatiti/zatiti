# P48 — Enforce full runtime and release gates in CI

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `.github/workflows`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P46, P47, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `.github/workflows`.

Implement in this order:

1. Extend existing CI/gate tooling to the updated contract/dependency pins and actual hosted/integration/desktop suites.
2. Enumerate every required case, not just tests that happen to emit a record. Treat missing tests, unconditional skips and stale evidence as blocked release claims.
3. Run Go baseline, renderer check, catalog parity, Flutter analyze/tests/live proof and native qualification on supported runners; retain bounded sanitized logs/artifacts.
4. Keep artifact assembly/qualification separate from publication/signing authorization and do not make tag creation a substitute for evidence.
5. Respect shared-machine heavy-build rules on self-hosted runners; standard hosted runners need their own concurrency/resource limits.

6. Track the platform regressions recorded in audit.md through P30 and require passing Linux/macOS evidence before claiming a healthy Go baseline; the local passing run does not supersede hosted failures.

Required behavioral tests:

- Deliberately omit or skip one required journey: release gate fails.
- A stale source revision or changed catalog/profile digest invalidates prior evidence.
- Healthy baseline, renderer and completed journey matrices pass with linked artifacts.

- Both supported hosted Go jobs exercise the symlink-directory and tampered-blob regression tests and retain their results.

Package check: `go test ./.github/workflows/cigate`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: CI gates that reflect product readiness rather than unit-test success. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
