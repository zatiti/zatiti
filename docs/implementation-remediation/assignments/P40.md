# P40 — Expose complete MCP work journeys

Status: planned. Priority: P1. Owner: `internal/mcp`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P37, P38, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/mcp`.

Implement in this order:

1. Regenerate MCP tool definitions from revision 3 catalog, including new start/history and recovery operations.
2. Keep tools backed by the authenticated common client; no model-supplied profile/actor switch or private internal-operation shortcut.
3. Preserve protocol-only stdout, structuredContent/text equivalence and domain-fault isError mapping.
4. Ensure long work remains discoverable/inspectable after the MCP client disconnects.

Required behavioral tests:

- Every public operation has one correct tool/schema and all new paths are callable through a real SDK client.
- Client reconnect inspects original task/command without duplication.
- Human-only review remains denied to an agent through MCP exactly as through CLI.

Package check: `go test ./internal/mcp`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: MCP parity for the complete runtime. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
