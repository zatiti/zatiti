# P37 — Regenerate complete capabilities and transport catalogs

Status: planned. Priority: P0. Owner: `internal/registry`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/registry`.

Implement in this order:

1. Register every revision 3 operation and strict schemas, with stable CLI/MCP paths, version/caller metadata and collision checks.
2. Distinguish operation availability from runtime readiness and qualified execution guarantees; a listed operation is not proof its provider/job runner is configured.
3. Generate OpenAPI and checked catalog fixtures from the same descriptors; include new task/history/identity/verification projections.
4. Keep compatibility explicit so older clients can show unsupported rather than silently guessing shapes.

Required behavioral tests:

- All declared operations register exactly once with resolvable schemas and transport mappings.
- Runtime readiness reports absent hosted/memory/runner prerequisites honestly.
- Old/new schema compatibility follows P00 and a changed operation version invalidates stale clients.

Package check: `go test ./internal/registry`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Revision 3 discovery/OpenAPI/catalog baseline. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
