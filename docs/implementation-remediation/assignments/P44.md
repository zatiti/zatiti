# P44 — Finish desktop memory, files, routines and capability details

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `apps/desktop`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P43, P28, P36, P21, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `apps/desktop`.

Implement in this order:

1. Replace prerequisite-only tabs with authorized memory claims/source/freshness/retract views when capability exists, keeping unsupported modes clearly visible.
2. Render artifact output names, task/run provenance, immutable digest and authorized sharing metadata; show verifier observations and failed checks.
3. Render responsibility-to-cycle/schedule/time-zone links and next-wake decisions rather than treating trigger strings as schedule descriptions.
4. Show cost liabilities, context-capture/advisory posture, autonomy evidence and recovery obligations using truthful controller projections.
5. Exercise keyboard/focus/semantics and native platform storage with populated live state.

Required behavioral tests:

- Populated live records render correct source, checks, costs and schedules; empty or unavailable states stay honest.
- Memory correction and artifact deletion/integrity failure update the UI without hiding retained audit evidence.
- Keyboard-only and assistive-technology journeys handle review/pause and reconnect.

Package check: `cd apps/desktop && dart format --output=none --set-exit-if-changed . && flutter analyze && flutter test`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Complete specified desktop detail surfaces. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
