# P30 — Support portable encrypted backup custody and blob restore

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `internal/platform`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/platform`.

Implement in this order:

1. Implement typed encrypted blob inventory/import and backup key-transfer mechanics fixed by P00 without exposing raw keys in manifests/API results.
2. Backups must be recoverable in a clean destination using explicit secure key provisioning; current opaque references to a different secret store are insufficient.
3. Verify every imported artifact digest and encrypt under the destination custody policy; retain restricted permissions and symlink defenses.
4. Keep failed restore staging isolated and cleanable without touching live user data.

5. Reproduce the platform failures from CI run 35473210732 on Linux and macOS: TestListenPrivateRefusesSymlinkedRunDirectory accepted a symlinked directory on both; TestBlobTamperedObjectFailsPublishOverExisting accepted tampered content on macOS. Establish the cause with filesystem fixtures before fixing implementation or fixture assumptions; preserve the rejection contracts.

Required behavioral tests:

- Backup key provisioned to a clean destination opens the bundle; missing key returns prerequisite_missing.
- Corrupt/truncated artifact, symlinked destination or wrong installation cannot replace live blobs.
- Secrets stay absent from ordinary logs, manifests and exported configuration.

- TestListenPrivateRefusesSymlinkedRunDirectory and TestBlobTamperedObjectFailsPublishOverExisting pass on Linux and macOS without skips or weakened error assertions.

Package check: `go test ./internal/platform`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Blob/key capabilities required for an actual portable restore. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
