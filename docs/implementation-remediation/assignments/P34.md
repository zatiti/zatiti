# P34 — Expose complete receipts, event tails and audit projections

Status: planned. Priority: P0. Owner: `internal/evidence`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/evidence`.

Implement in this order:

1. Add a stable resumable event-tail cursor even on a drained page; bind it to principal/scope/filter/retention semantics.
2. Project turn/proposal/task/run/effect/verification linkage from committed evidence, including waiting and unknown dispositions, never from worker claims.
3. Provide retained command/result and job completion links for clients to resolve lost acknowledgements and later outcomes.
4. Preserve filter authorization, cursor expiry and no-secret output across new projections.

Required behavioral tests:

- A client drains events, restarts, then resumes without missing or endlessly rereading events.
- Foreign/revoked scope cannot use an earlier cursor; expired cursor returns the named recovery path.
- Receipt for an accepted effect remains pending until authoritative confirmation and never implies verified task success.

Package check: `go test ./internal/evidence`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Usable event streaming and truthful end-to-end evidence. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
