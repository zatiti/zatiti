# P36 — Drive earned-autonomy evidence updates

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `internal/policy`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P07, P18, P34, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/policy`.

Implement in this order:

1. Connect verified result/incident events to autonomy evaluation and immediate demotion with durable deduplication/cursors.
2. Require pinned evidence identity, verifier versions, freshness and unchanged dependency closure; model reports and stale tests are not qualification.
3. Keep qualification below the configured ceiling and preserve human-required classes.
4. Expose why a capability was earned, lost or needs requalification for the desktop.

Required behavioral tests:

- Verified qualifying results can promote only within the ceiling.
- Relevant version change or incident invalidates qualification before the next effect admission.
- Worker assertions/self-qualification and an unrelated dependency change behave according to the existing Z19 acceptance cases.

Package check: `go test ./internal/policy`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Operational autonomy updates rather than inspectable rules alone. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
