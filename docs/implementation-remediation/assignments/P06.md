# P06 — Complete connection setup and adapter validation actions

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `internal/connections`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/connections`.

Implement in this order:

1. Define executable tool contracts for model calls, public HTTP reads, repository operations and supported memory reads using exact adapter schemas, separate from ExecutionProfile IDs.
2. Complete the secure-helper receipt handoff: custody a real reference, bind receipt to challenge/account/destinations/expiry and atomically apply the verified credential reference at Finish.
3. Generate connection.validate actions supported by the selected adapter; do not send an invented generic probe to adapters that accept only domain-specific actions.
4. Persist validation evidence/freshness and callback ownership, and complete the execution-side job when validation records.
5. Expose setup/probe prerequisites and blocked capabilities through discovery without secret bytes.

Required behavioral tests:

- Real helper fixture completes a challenge; wrong account, replay, expiry and changed connection refuse.
- Every built-in validation action passes its adapter's strict schema and completes job.get.
- Validation freshness expires and stale validation cannot activate a changed tool or connection.

Package check: `go test ./internal/connections`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Usable setup/validation lifecycle and pinned executable tool mappings. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
