# P08 — Close runtime budget and reservation accounting

Status: planned. Priority: P0. Owner: `internal/accounting`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/accounting`.

Implement in this order:

1. Specify reservation ownership for root task, attempt, physical model/tool call, evaluation and responsibility cycle; avoid double-reserving the same ceiling as independent spend.
2. Enforce shared root/ancestor limits across parallel children and model calls; settle real provider usage once by operation/attempt identity.
3. Preserve unknown/advisory liabilities across report, cancellation, retry and restore; verification success is not provider cost evidence.
4. Expose aggregate spending, outstanding reservations and reasons a worker cannot proceed for doctor and desktop projections.

Required behavioral tests:

- Two competing children cannot exceed one root/ancestor budget.
- Duplicate observation settlement is idempotent; lost billing leaves the reservation visible.
- Many empty reasoning cycles stop at the configured aggregate bound; zero-spend bootstrap remains usable.

Package check: `go test ./internal/accounting`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Runtime accounting rules proven with competing actual owner transactions. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
