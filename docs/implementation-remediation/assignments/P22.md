# P22 — Drive turns, contexts and model/tool work in the controller

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `internal/controller`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P16, P17, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/controller`.

Implement in this order:

1. Extend tick with bounded fair work phases: discover worker recipients, atomically admit turns, claim hosted work, prepare/publish context, prepare effects, settle observations and resume eligible turns.
2. Use internal owner ports; never query another package's tables or bypass worker authority. Remove the monitoring-only ready scan as the sole stranded-task response.
3. Journal every physical-call boundary, publication and callback before advancing; shutdown/restart preserves already admitted work and generation fencing.
4. Use explicit stored route/mode from effects; provider parameters remain strict. Dispatch Invoke versus Reconcile correctly once P23 provides reconciliation work.
5. Publish actionable controller obligations/health for missing profiles, blocked jobs and stalled turns with scoped IDs and no secret logs.

Required behavioral tests:

- A real message plus controlled model reaches a reply and verified task result while no client stays open.
- Crash before claim, after context publication and after provider response resumes safely without duplicate model/tool mutations.
- Blocked work beyond one batch does not starve an eligible later item; pause closes admission promptly.

Package check: `go test ./internal/controller`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: The production autonomous worker driver. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
