# P09 — Publish named outputs and context artifacts durably

Status: planned. Priority: P0. Owner: `internal/artifacts`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/artifacts`.

Implement in this order:

1. Implement artifact metadata/provenance additions for output name, task/run/attempt, producing operation and source artifacts; validate scope ownership through the frozen ports.
2. Support the staged-to-published handoff for contexts, model text, tool results, verifier requests and verdicts using stable source identities.
3. Bind an output name to one published artifact digest for a reported attempt; replacement requires a new report/attempt version and never rewrites accepted checks.
4. Provide bounded enumerations/pins for backups and exports, integrity verification and abandoned-staging cleanup with recovery-safe retention.

Required behavioral tests:

- Crash between blob publish and metadata commit can replay without a duplicate artifact or orphaned result reference.
- A task cannot bind a foreign artifact or replace another attempt's output.
- Reported output bytes remain readable after restart; missing/corrupt bytes fail verification and backup.

Package check: `go test ./internal/artifacts`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Named output/provenance registry and reusable durable publication. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
