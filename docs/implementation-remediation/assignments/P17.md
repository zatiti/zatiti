# P17 — Complete responsibility reasoning and event wakes

Status: planned. Priority: P0. Owner: `internal/scheduling`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P14, P11, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/scheduling`.

Implement in this order:

1. Consume durable cycle results from execution containing source responsibility/version, artifacts, child task IDs and bounded next-wake decision.
2. Validate useful-work/no-work/wait/escalate outcomes; record cycle and arm the next wake atomically, idempotent by cycle turn identity.
3. Implement authenticated event/signal and reply/dependency triggers with persisted source-event cursors, not arbitrary string matching or polling every model tick.
4. Recheck pause/cancel, minimum reconsideration intervals, DST/misfire and aggregate limits at wake admission; cap catch-up enumeration and batch work.
5. Make responsibility source linkage visible on cycle tasks and schedules so desktop can show real schedule/time-zone relationships.

Required behavioral tests:

- First cycle creates bounded work and a second wake without manual _scheduling.cycle.record.
- No-work decisions consume bounded accounted spend; duplicate event/restart does not repeat a cycle.
- Pause versus due-wake and reply versus wake races admit at most the permitted work.

Package check: `go test ./internal/scheduling`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: An ongoing-work cycle that actually recurs and remains bounded. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
