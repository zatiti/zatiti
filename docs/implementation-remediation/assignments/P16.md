# P16 — Interpret model responses into bounded governed work

Status: planned. Priority: P0. Owner: `internal/execution`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P15, P04, P12, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/execution`.

Implement in this order:

1. Replace unconditional prepare-next-model-step behavior with the decision state machine in contract-proposals.md: reply/clarify, local proposal, external proposal, delegate, wait, complete, refused/incomplete.
2. Strictly validate normalized ModelOutput and source_context against the dispatched request. Admit proposals via the worker operator and deterministic turn/proposal keys; resolve current tool/connection/bindings independently of model text.
3. For local management operations drive existing draft/plan/apply/task/responsibility APIs under worker authority. Report missing prerequisites or exact reviews as structured pending state and human-facing messages.
4. For external tools prepare effects and await their observation; append published tool result and resume with a new context. Never treat accepted as confirmed success.
5. Publish final reply/output artifacts, record report through the trusted internal equivalent of attempt.report and stop on completion, cancellation, timeout, root/model-step bound or unresolved effect. Heartbeats extend only an eligible live lease.

Required behavioral tests:

- Controlled model requests organization/task creation, a HTTP read, then a final report; real domain owners receive the calls and one final reply appears.
- Duplicate proposal, injection in fetched content, fabricated human approval and unauthorized connection cannot produce effects.
- Final output stops model calls; accepted/unknown external effect blocks dependent work; malformed output yields a durable bounded failure.

Package check: `go test ./internal/execution`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: The missing intent-to-work interpreter and useful hosted loop. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
