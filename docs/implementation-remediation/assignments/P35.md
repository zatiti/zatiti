# P35 — Close review lifecycle for live worker proposals

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `internal/reviews`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P07, P12, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/reviews`.

Implement in this order:

1. Exercise exact reviews created by real hosted proposals, retaining proposer, immutable action digest, account/destination and content artifacts.
2. Provide readable reason/title plus pending/resolved review links for waiting turns; current eligibility and human-required policy remain authoritative.
3. Version/revoke/expire reviews and wake the corresponding turn only after the decision is committed; stale or denied reviews cannot trigger dispatch.
4. Keep delegated reviewer bounds independent of model text and preserve task/effect uncertainty when an operator cancels.

Required behavioral tests:

- Human approves one exact live proposal and only that action runs; changed content/head/account refuses.
- Agent claiming approved_by_human cannot decide a human-required review.
- Expired/revoked/duplicate decision produces no extra action or reply.

Package check: `go test ./internal/reviews`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Live review completion/wake integration. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
