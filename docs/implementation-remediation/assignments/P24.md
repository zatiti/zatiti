# P24 — Wire the implemented runtime and secure setup helper

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `cmd/zatiti`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P23, P06, P13, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `cmd/zatiti`.

Implement in this order:

1. Import/register responses.New and remove the stale unimplemented-adapter entry; load only valid explicit profiles.
2. Construct trusted verifier, local job adapters and worker operator; attach every supported collaborator in superviseController instead of only Identity/Blobs.
3. Implement the documented installation-time helper mechanism for credential import/consent receipts using secure local IO; profile/account secrets never enter normal operation JSON or model context.
4. Expose startup doctor requirements when provider, price, currency, tools, helper or runner are missing. Test registrations against the operation/job catalogs.
5. Keep bootstrap usable with missing paid providers and make readiness diagnostics distinguish storage-only, chat-ready and task-ready.

Required behavioral tests:

- The production binary accepts a valid Responses profile, starts an actual controlled model turn and attaches the real verifier.
- Supported jobs are registered; a catalog-supported job lacking a runner fails an assembly test.
- Fresh setup through CLI/MCP helper mechanics establishes an opaque usable connection without printing secrets.

Package check: `go test ./cmd/zatiti`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Production wiring and a workable credential-setup entrypoint. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
