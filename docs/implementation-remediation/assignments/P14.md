# P14 — Implement durable worker turns and automatic hosted claims

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `internal/execution`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P03, P05, P08, P10, P11, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/execution`.

Implement in this order:

1. Create execution-owned turns for chat, task and responsibility triggers; use unique trigger/recipient identity and persist accountable source actor, worker, scope, limits and configuration revision.
2. Admit queued hosted runs automatically through the same fenced claim rules as cooperative run.claim; never auto-claim cooperative runs as hosted.
3. Add bounded/fair pending and claim operations for controller. Claim/run generation, lease expiry, worker pause and installation pause must be durable.
4. Allow bounded chief conversation turns before a task exists so the chief can clarify or propose configuration; do not require a task merely to create the first task.
5. Persist typed waiting reasons for missing provider, clarification, review, tool completion, dependency and budget; wakes revalidate current state.

Required behavioral tests:

- Message to an idle chief creates one durable turn; a duplicate creates none.
- Queued hosted run gets an attempt without a human calling run.claim; cooperative run stays available to its external worker.
- Controller restart fences stale claims, respects pause/cancel and does not reset a root's cumulative limits.

Package check: `go test ./internal/execution`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: A durable intake/claim state machine shared by chat and task execution. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
