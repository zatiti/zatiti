# P28 — Complete memory lifecycle against the qualified adapter

Status: blocked_external (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `internal/memory`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P27, P16, P02, P26. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/memory`.

Implement in this order:

1. Connect selection/recall/curation/promotion/retraction jobs to the real adapter modes and record normalized result artifacts.
2. Reconcile unknown memory writes by stable command identity and propagate corrections to derived/promoted claims without overwriting provenance.
3. Return actionable setup prerequisites if the installation's personal/org/worker brain has no qualified binding.
4. Integrate actual selected memory into hosted contexts while preserving current revocation and classification checks.

Required behavioral tests:

- A scoped turn recalls real selected claims with source refs and later audit reads exact old context.
- Correction/retraction survives restart and never leaves a promoted claim falsely current.
- Hard-bound mode refuses advisory/unsupported provider profiles.

Package check: `go test ./internal/memory`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: End-to-end usable memory, gated on explicit upstream capability evidence. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
