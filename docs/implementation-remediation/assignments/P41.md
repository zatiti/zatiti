# P41 — Verify transport admission and new runtime lifecycle boundaries

Status: planned. Priority: P1. Owner: `internal/server`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P37, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/server`.

Implement in this order:

1. Serve revision 3 operations through the unchanged authenticated envelope and enforce exact version/error HTTP mapping.
2. Integrate quiesce/restore admission states: queries remain intentionally scoped, prohibited mutations refuse, bootstrap cannot reopen after initialization.
3. Keep local credential and remote certificate-principal mapping explicit; identity.current must use the actual authenticated caller.
4. Add bounded long-work submission/disconnect tests without keeping server request contexts alive as task lifetimes.

Required behavioral tests:

- Disconnect after task submission does not cancel durable work or repeat it.
- Restoring/paused installation refuses forbidden operations with named faults.
- Remote mTLS maps the correct principal and cannot spoof a local-human actor.

Package check: `go test ./internal/server`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Transport support and verification for runtime/restore boundaries. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
