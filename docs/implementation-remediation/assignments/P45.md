# P45 — Produce usable release and installation tooling

Status: implemented_open_qualification (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `packaging`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P24, P33, P43, P27, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `packaging`.

Implement in this order:

1. Build on the existing manifest/signature/install library: add a documented executable packaging/install driver within the allowed root and reproducible controller/desktop release assembly.
2. Include the qualified secure helper, adapter profiles, Serenity runtime/read facade and exact licenses/SBOM; missing mandatory memory guarantees block the full release.
3. Drive real per-user launchd/systemd lifecycle and secure key provisioning; separate controller/desktop packages so closing UI does not stop work.
4. Validate upgrade, rollback, uninstall and state retention. Signing/notarization must consume authorized external identities and retain evidence; do not publish as part of this coding assignment.
5. Document clean-host installation and restoration with accurate supported OS/architecture list; Windows is unqualified until an explicit release scope decision and evidence.

Required behavioral tests:

- Install produced artifact on clean macOS/Linux, bootstrap, run first work, upgrade/restart, back up/restore and uninstall according to docs.
- Tampered manifest/binary/helper/profile is refused before service start.
- Uninstall preserves or removes state only according to explicit user choice and permissions remain private.

Package check: `go test ./packaging`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Installable reproducible artifacts and executable distribution qualification inputs. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
