# P39 — Expose complete CLI work journeys

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `internal/cli`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P37, P38, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/cli`.

Implement in this order:

1. Generate commands for new catalog operations; preserve --input, --json, submission key and exit semantics.
2. Provide discoverable start/inspect/wait paths through public operations without a TTY or hidden confirmation.
3. Render accepted versus completed versus blocked/unknown results distinctly and keep diagnostics on stderr.
4. Add CLI-local fixtures for new input/output shapes; cross-package process journeys belong to P46.

Required behavioral tests:

- New task/history/current-identity commands resolve to exact catalog operations.
- Accepted job and unknown mutation are never printed as successful task completion.
- JSON mode emits one envelope and retains original command identity on replay.

Package check: `go test ./internal/cli`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: CLI parity for useful work and honest long-running results. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
