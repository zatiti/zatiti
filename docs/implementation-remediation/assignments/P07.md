# P07 — Enforce authority on planned model and tool actions

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `internal/policy`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/policy`.

Implement in this order:

1. Use the frozen worker-subject and context disclosure contracts to intersect installation/org/project/task/worker authority for every model call and proposal.
2. Validate all resolved context classifications and destinations before model dispatch, including skills, attachments, tool results and memory excerpts.
3. Bind exact reviews to final action bytes, connection account/version, destination, time window and repository preconditions; no broad conversational approval may stand in for review.decide.
4. Return actionable prerequisite/review/deny results that the hosted loop can persist and wait on without busy retry.

Required behavioral tests:

- Empty tool bindings allow no external tool call even if discovery lists it.
- A restricted attachment prevents a public-model request before any provider call.
- Content/account/head changes invalidate the earlier review and require a new exact decision.

Package check: `go test ./internal/policy`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Policy integration for the real hosted loop, retaining deny-wins and human review. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
