# P19 — Run sealed skill evaluations to completion

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `internal/skills`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P18, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/skills`.

Implement in this order:

1. Implement the frozen local job executor for skill.evaluate, preserving its accepted skill/version, fixtures, posture, limits and verifier profile.
2. Use published fixture artifacts and real verification rather than storing pending evaluation metadata forever.
3. Wire terminal results back into the skills-owned evaluation row and execution job; attach observation/evidence refs so activation can use them.
4. Dry-run, sandbox and advisory posture must expose their actual guarantees; never advertise unavailable containment.
5. Deduplicate claims/results and keep cancellation, unknown effects and changed skill bytes visible.

Required behavioral tests:

- An imported skill's evaluation job finishes and activation consumes its real evidence.
- Failing fixture blocks activation; changed skill version cannot reuse old qualification.
- Restart during evaluation does not rerun an uncertain external action or invent a pass.

Package check: `go test ./internal/skills`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Executable skill evaluation jobs and owner completion callbacks. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
