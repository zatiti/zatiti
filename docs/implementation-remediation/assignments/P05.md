# P05 — Resolve complete immutable worker configuration

Status: implemented (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P0. Owner: `internal/configuration`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/configuration`.

Implement in this order:

1. Extend snapshots to resolve the exact worker, principal binding, instructions, skill version closure, tool bindings, execution profile, connection versions and classification constraints needed by context assembly.
2. Implement configuration prerequisites for runnable hosted workers; a missing profile/provider/budget must yield named diagnostics and remain unready.
3. Ensure chief/team creation through plans provisions identities and conversations through explicit owner ports, atomically with configuration changes.
4. Move organization/team/project export off the query transaction into the common durable/local-IO pipeline; register both export job and artifact metadata rather than returning invented IDs.
5. Preserve canonical import/export exclusions and cross-installation rebinding; export may not include secrets or execution history.

Required behavioral tests:

- A new worker snapshot resolves all pinned dependencies, and a stale connection version blocks apply/dispatch.
- Export result is readable through job.get and artifact.read and survives restart.
- Organization export/import/export preserves definitions; exported credential references require explicit rebinding on another installation.

Package check: `go test ./internal/configuration`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Runnable worker snapshots, honest prerequisites and durable configuration exports. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
