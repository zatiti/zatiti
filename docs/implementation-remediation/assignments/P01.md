# P01 — Implement shared types and strict validation for the new seams

Status: planned. Priority: P0. Owner: `internal/contract`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P00. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/contract`.

This is a serialized shared/foundation assignment. No child-package writers may run while it changes shared files or generated prompts.

Implement in this order:

1. Implement only the revision 3 shared DTOs/interfaces and schema primitives; preserve existing fault/result and canonicalization behavior.
2. Add worker-subject execution, job claim/result, context work item, output-slot binding, restore snapshot/overlay types and discriminated model decision validation.
3. Reject unknown fields, duplicate keys, invalid discriminator combinations and widened scopes; keep ordered transcript/proposal arrays ordered while canonicalizing declared sets.
4. Provide fixture-based contract tests that all package owners can reuse without importing sibling implementations.

Required behavioral tests:

- All P00 golden documents round-trip and canonicalize identically.
- Malformed model decisions cannot smuggle arbitrary operations, identity, grants or review decisions.
- Revision 2 persisted envelopes remain readable under the declared compatibility policy.

Package check: `go test ./internal/contract`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Shared revision 3 baseline and executable schema fixtures. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
