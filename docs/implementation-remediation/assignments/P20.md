# P20 — Implement controlled repository verification

Status: planned. Priority: P1. Owner: `internal/execution`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P18, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `internal/execution`.

Implement in this order:

1. Implement repository_patch_applies and repository_command using the controlled runner/profile fixed by P00/P02 inside the execution root.
2. Checkout the pinned repository/base/tree into a fresh disposable workspace; apply only the reported patch artifact and run only accepted argv/env/check definitions.
3. Bound wall time, child processes, output bytes, disk and permitted network according to the declared runner capability; do not call ordinary subprocess isolation a sandbox.
4. Capture base/head, patch hash, runner/version, exit status and output artifacts as independent evidence; clean up only the runner's own workspace.
5. If a required containment guarantee is unavailable, refuse that profile with a named prerequisite and preserve the task result as unverified.

Required behavioral tests:

- Valid patch/check passes; nonapplying patch, failing command and wrong repository head fail independently.
- Timeout terminates the owned process group and retains bounded diagnostics.
- Worker-authored passing logs or a modified verifier command cannot satisfy the sealed checks.

Package check: `go test ./internal/execution`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Engineering-task verification, with precise supported runner guarantees. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
