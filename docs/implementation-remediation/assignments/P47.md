# P47 — Replace unconditional qualification skips with executable harnesses

Status: implemented_open_qualification (current ledger as of 2026-09-25). Historical assignment; no active dispatch. Priority: P1. Owner: `tests/qualification`.

This is a coding assignment, not evidence that its behavior exists. Read the root guidance, this directory's current AGENTS.md, and the landed revision 3 schemas before editing. The remediation plan does not itself change the frozen contracts.

Start only after these assignments are landed: P46, P20, P28, P33, P44, P45, P02. Same-owner assignments never run concurrently. Use an isolated worktree and one integration owner; do not edit sibling roots, shared dependencies or generated prompts. Raise a concrete contract change to P00/integration instead of inventing a seam.

Allowed writes: `tests/qualification`.

Implement in this order:

1. Replace Responses' unconditional notRun with a pinned controlled-protocol harness and separately authorized real-provider qualification; record actual endpoint/model/price/limits evidence.
2. Launch the built Flutter application with an integration_test/native driver for all Z21 and daily-work journeys; widget tests alone do not qualify the built app.
3. Install produced artifacts on clean macOS/Linux hosts and execute service restart/backup/restore/uninstall journeys.
4. Run actual claimed named-agent clients only in explicitly authorized billed/network sessions; missing credentials or host must be recorded as not_run and block that claim.
5. Map every one of the 116 existing cases and added cases to executable test identity, source revision, fixtures, environment and evidence; omissions and skips cannot pass release qualification.

Required behavioral tests:

- No unconditional notRun branch remains where a supported harness can run.
- Required release cases with missing, skipped, failed or stale evidence fail the release gate.
- Report distinguishes simulator/protocol checks, real service guarantees and native platform/client evidence.

Package check: `go test ./tests/qualification`. New test names must identify the behavior above; merely testing a fake implementation of the new behavior is not completion. Run real owner integration at landing. Tests must fail if the protected transition/dispatch is removed or doubled.

Before multi-package Go/build checks, inspect uptime and acquire the project-required R-build-lease; hold if load exceeds 10, use the canonical claim script, retain the returned SHA and release immediately after the command. Do not run a broad race suite in parallel. Missing credentials/providers/native hosts are not passing tests.

Handoff: Executable external/native qualification and a claim-specific release evidence report. Include changed files, migrations, exact commands/results, new test identities, prerequisite blockers and caller compatibility. Preserve secret custody, current authorization, immutable accepted contracts, external outcome uncertainty, and CLI/MCP parity. No publishing or deployment is authorized by this card.
