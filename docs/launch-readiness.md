# Zatiti launch readiness

Status as of 2026-09-26: **pre-release; Mac install-to-first-chat is not qualified.** The P00–P50 implementation dispatch is closed. The open work below is the current product/release roadmap; old cards are historical records, not active branches.

## Remaining work plan

Implementation dispatch P00–P50 is closed. These are new release-qualification workstreams, with one write owner per root. Work in separate rows may proceed concurrently only when it does not edit a shared contract or root dependency file. In particular, the Zatiti adapter and governed-context work cannot claim completion before the hosted Serenity contract and deployed capability statement exist.

| Workstream | Owner / write roots | Can run alongside | Exit evidence |
|---|---|---|---|
| **S1. Serenity public contract and hosted deployment** | Serenity integration owner; `internal/hosted/oauth`, `internal/hosted/service`, `internal/hosted/gateway`, public memory protocol/server, protocol schemas | P1 and P2 preparation below | A token-bound binding read returns verified issuer/resource, stable account, selected project, effective scopes, grant and current revocation/project state. Public memory contract covers governed claim identity/provenance, freshness, cost/disclosure, idempotent command identity, authoritative command status, source/index revisions, and immutable export/restore by revision. Contract tests cover wrong account/project/scope, revoked grants, stale state, lost acknowledgments and revision pinning. A deployed build identity is tied to the tested source and a capability statement is published. |
| **P1. Provider live qualification preparation** | Zatiti qualification owner; `tests/qualification` (existing gated OpenAI test is not sufficient for the selected profile) | S1, P2 | After the previously exposed Experiential Labs key is rotated, provision a fresh credential only into the approved local secure store. Run one bounded qualification probe for the selected provider/model/route and explicit spend ceiling, then one synthetic first chat using the reviewed pinned profile. Read the response from durable controller conversation history and again after restart. Record redacted build/profile/model/route/usage/evidence IDs and physical request count. No retry after an unknown outcome. |
| **P2. Mac release pipeline preparation** | Zatiti release owner; `packaging`, `.github/workflows`, signed artifact production | S1, P1 preparation | Produce Intel and Apple Silicon controller, desktop, helper, installer, bootstrap and signed release descriptor/delivery bytes; verify Developer ID, notarization, staple, hashes and Team ID pin. Implement/register the native clean-host journey driver. The current machine has Developer ID Application and Installer identities, but notarization credentials/profile were not verified; Apple Silicon CI is not a clean-host install driver. |
| **S2. Zatiti hosted OAuth binding** | Zatiti integration owner; `cmd/zatiti`, `cmd/zatiti/credential_helper`, `internal/platform`, `internal/connections` | P1/P2 preparation; starts after S1 binding contract is reviewed | Discover metadata; register public client; perform S256 PKCE with exact numeric-loopback callback/state validation; exchange/rotate/revoke tokens in Keychain; recover from crashes/unknown outcomes. Persist only binding metadata obtained from S1's authenticated token-bound endpoint. Display the verified existing project and scopes for explicit confirmation. Controlled callback/redaction/recovery tests pass, followed by browser sign-in, exact brain/scope verification, reconnect and revoke evidence. |
| **S3. Zatiti Serenity adapter** | Zatiti owner; `internal/adapters/serenity` | S2 implementation may be prepared against the reviewed contract, but final qualification follows S1/S2 | Implement hosted MCP handshake and scoped operations; check current token-to-project binding before dispatch; reconcile lost acknowledgments via authoritative command status; keep unsupported or stale operations fail-closed. Controlled exact-request/count tests pass, then live deployed-service recall/write/reconciliation/cost/disclosure/revocation/revision-export tests pass. Any shared contract or dependency-injection change is serialized through the integration owner. |
| **S4. Governed recall in model context** | Zatiti execution/memory integration owner; `internal/memory`, `internal/execution` | Independent tests/fixtures can be prepared during S1, but production wiring follows S3 | Only completed, authorized, fresh, correctly classified and lineage-bearing recall enters the transaction-pinned immutable context artifact. Failed, partial, stale, revoked or interrupted recall fails closed. Tests prove admission/commit behavior; final evidence is a real hosted recall plus provider reply read from durable conversation history. |
| **S5. Native Intel and Apple Silicon qualification** | Zatiti qualification owner; `tests/qualification` | Starts when S2–S4 and P1/P2 artifacts are ready | On each clean native host, run the literal one-line install with no developer tools/Rosetta. One host-run report binds identical release identity and shared hashes to architecture-specific signed package/component/helper hashes, installed inbox and active trees, retained Keychain state, browser OAuth and verified existing brain, bounded provider profile, durable first reply, full Serenity guarantees, update/rollback/uninstall/state retention and backup restore. Both reports pass the same required case IDs. The production driver currently is unregistered, so this gate is not run evidence today. |
| **S6. Recovery/support and release review** | Release integration owner; `docs/launch-readiness.md`, support/recovery docs and release evidence bundle | Draft content may be prepared during S1–S5; final review is serialized | Verify user-facing reconnect/key rotation, backup/restore, recovery and support procedures against the release build; resolve applicable observability/schema-version findings. Publish only when S1–S5 evidence is complete and the final release report has no missing, skipped or `not_run` required case. |

The permitted parallel plan is therefore S1, P1, and P2 as separate preparations; then S2 can start against the reviewed S1 contract, S3 follows the binding API, S4 follows the working adapter, and S5 follows all live service/provider/artifact prerequisites. S6's documentation can be drafted in parallel, but release approval remains the final serialized step. No reduced-capability Mac preview satisfies the current requirements.

## Current blockers and exact gaps

- **Serenity (hard upstream dependency):** the local source checkout is clean at `b4febdf` and matches Zatiti's pin. It implements metadata at `/.well-known/oauth-authorization-server` and `/.well-known/oauth-protected-resource`, dynamic registration at `POST /oauth/register`, PKCE authorization at `GET /oauth/authorize`, token/refresh at `POST /oauth/token`, revocation at `POST /oauth/revoke`, and project-scoped MCP. It does not expose a bearer-authenticated binding read that jointly proves stable account, selected project, effective scopes, grant and live revocation/project state. Its current `recall`, `remember`, `entity`, `synthesize`, and `forget` MCP surface also does not yet prove Zatiti's required provenance/freshness, bounded cost/disclosure, lost-ack status reconciliation, or immutable revision export/restore. Local source tests and synthetic empty memory are not deployed hosted-service evidence.
- **Zatiti OAuth/memory:** `internal/connections/localio.go` consumes pre-supplied challenge metadata rather than doing discovery/registration/PKCE/callback/token lifecycle. `internal/adapters/serenity` refuses physical operations and reconciliation. `internal/execution/context_build.go` fails closed when configured recall is incomplete; completed governed recall is not yet part of the immutable model context. These are explicit code gaps, not merely a request for credentials.
- **Provider:** OpenAI, OpenRouter and Experiential Labs adapters and the desktop model/profile workflow have controlled tests. There is no real installed Desktop → controller → provider → durable conversation readback/restart result. The existing opt-in live test is OpenAI-specific and is not evidence for OpenRouter or Experiential Labs. The exposed Experiential Labs credential must be rotated before any use; its prior value must not be reused.
- **Mac:** this Intel machine has valid Developer ID Application and Installer identities. Notarization credentials/profile were not verified. No signed/notarized final packages or bootstrap are present, and `liveMacHostDriver` is unregistered. Native Apple Silicon CI builds/tests source but does not install the release on a clean host. Consequently both Intel and Apple Silicon install-to-first-chat remain unqualified.

Until these gates pass, keep the draft PR and README marked pre-release and do not publish a supported install command. iOS, Android, web, Linux, and Windows remain later qualification tracks.

## Evidence available now

- The integrated full Go suite passed: `go test -p 2 ./... -count=1 -timeout=20m`.
- `go vet ./...` passed.
- Draft PR CI passed on Ubuntu, native Apple Silicon macOS, and native Intel macOS for build, full unit/integration tests, and the Flutter desktop matrix. The race suite passed on Ubuntu and Apple Silicon; Intel ran the full unit/integration suite, with its race step omitted because the same full suite exceeded the 40-minute race timeout there.
- The spec renderer check passed for 41 files, 37 scopes, 305 operations, 153 source blocks, and 147 cases. The generated MCP runtime mirror check and its synchronization tests passed.
- Flutter analyze and all 243 desktop tests passed from `apps/desktop`.
- The opt-in OpenAI live qualification test compiles and its default gated path passes; it did not make a live request.
- No real hosted Serenity recall, live provider first-chat, signed/notarized Mac installation, or clean Intel/Apple Silicon host run has been recorded in this work. The Go macOS distribution harness is controlled packaging evidence, not native signed-install evidence.

These results demonstrate source-level and controlled-test progress only. They do not close the launch gates.

## Repository integration status

Local `main` includes the reconciled `origin/main` history plus 83 integrated implementation commits, with no remote-only commits. Draft PR [#71](https://github.com/zatiti/zatiti/pull/71) contains the reviewed integration and follow-up CI fixes, and its full CI matrix passes. The only local topic branch is the active PR branch; there are no side worktrees or abandoned local implementation branches. The stale voice-mode worktree metadata and local alias were removed after verifying they were an ancestor of the integrated PR branch; the closed, unmerged Z-M2 implementation branch was deleted after verifying its implementation is integrated. The corresponding PRs remain as audit records. The GitHub description and marketing README describe the current pre-release status. Remote merged topic refs remain as history, not active work. This draft PR is the review step, not a release qualification claim.
