# Sitrep — 2026-09-20 — Zatiti implementation-remediation

## What you need to do

Nothing needs you right now. Every open thread either has an owner and is
moving (the three remaining cards from today's parallel batch), or is
genuinely self-serviceable by this session (the small contract cleanup
below) rather than something only you can decide. The one thing worth your
attention, not your action, is in Risks: a real security bug was found and
fixed today, and you should know about it even though it doesn't need a
decision from you.

## Verdict

This effort is on track and moving fast, but it is still working through
preparatory plumbing, not the actual missing product. Today closed out
"wave 3" — the second-largest batch of foundational packages (identity,
configuration, connections, policy, accounting, artifacts, messaging,
tasks, memory, evidence, the shared operation catalog, and three more
landing within the hour) — cleanly, with two real bugs found and fixed
along the way (one a security vulnerability, described below). The
central thing the original audit found missing — a durable loop that lets
a worker actually do useful, verified work end to end — has not been
started yet. That is next, not now.

## Overall: ~30% (first report) — wave 3 (package-level groundwork) essentially done, wave 4 (the actual worker loop) not started

This plan (called "implementation-remediation" in this repo — a fresh
audit run on 2026-09-19 that found the control plane for this product
exists, but the piece that turns an admitted request into independently
verified work does not) breaks the remaining work into 50 numbered cards
("P00" through "P49"), each one package's worth of remediation, grouped
into 20 batches ("waves") by dependency order, and further grouped into 5
milestones (M0 through M4) marking real product capability, not just code
landing. 30% counts cards merged to the main branch with their own tests
passing, out of 50 total — a simple, even weighting, since each card is
scoped to be roughly one package's worth of work. It is the honest
measure because the alternative — counting waves or milestones — would
overstate progress: wave 3 is the largest wave by card count but is
entry-level plumbing (make each domain package speak the new shared
contract correctly), not the "chief can do useful work" milestone (M1)
that actually matters, which hasn't been touched.

## The four states

**Done: 30%** (15 of 50 cards merged with passing tests). This is milestone
M0 (the shared contract is frozen and internally consistent) plus 12 of
wave 3's 15 packages:
- The shared contract itself was frozen to its third revision and two
  real gaps were caught and fixed during that work: a stale dependency
  claim (a retired GUI toolkit) was cleaned out of the dependency ledger,
  and a missing piece of the object-storage lock file was reconciled.
- The shared Go types every other package builds on were implemented and
  matched to the frozen contract.
- Twelve of wave 3's fifteen packages are merged: worker identity binding
  (which also fixed a real access-control bug — a narrowly-scoped worker
  could previously read or revoke the account owner's own identity record
  by guessing its ID, now fixed and proven with a test), worker
  configuration snapshots, connection setup/validation, policy
  enforcement on model and tool calls, budget/spend accounting, durable
  output publishing, message delivery and chat history, the task
  start/lifecycle machinery, the hosted-model adapter's session handling,
  local memory projections (which also fixed a real data-typing bug in
  how memory records point at stored artifacts), the durable
  event/audit trail, and the central operation catalog that every other
  package's requests get checked against.

**In progress: 6%** (3 of 50 cards — effect reconciliation, database
restore mechanics, and a security-relevant backup/platform fix — all
implemented and verified, landing one at a time right now). This session
runs each package's coding work as a separate background worker, but
lands only one at a time to a shared branch — a timing mistake earlier
today (two workers committing at once) briefly drove the build machine's
load far past its safe threshold, so every commit now goes through a
single queue instead. Concretely:
- Effect reconciliation and callback routing (fixes a case where a lost
  network response to a paid action could previously be re-sent instead
  of safely re-checked) — implemented, queued to land.
- Database snapshot/restore mechanics (crash-safe backup swap with
  recovery journaling) — implemented, queued to land.
- Platform-level backup key handling, plus **a real security bug found
  and fixed**: a permission check meant to refuse a symlinked directory
  (the kind of thing an attacker could plant to redirect where a private
  socket file gets created) was silently bypassing itself under a
  specific path-resolution order. It has always been present; today's
  audit-driven review is what caught it, and the fix has been verified
  200 times in a row without a miss. A second test that looked like a
  companion security failure was investigated with equal rigor and
  turned out to be a coin-flip test-fixture bug, not a real
  vulnerability — also fixed. Landing shortly.

**Todo: ~62%** (roughly 31-32 of 50 cards, not yet started). This is
almost the entire rest of the plan, including the part that matters
most: the durable worker loop (a chief assigns work, a worker's context
gets built, the model responds, the response gets interpreted into
bounded governed actions, and the result gets independently verified) —
six cards, none started, all now unblocked by today's wave-3 landings.
Also queued: the controller that drives that loop end to end, wiring the
finished runtime into the actual command-line/server binary, skill
evaluation, cooperative task recovery, the desktop chat client, release
packaging, the full proof-of-every-operation test suite, and CI
enforcement gates.

**Blocked: ~2%** (roughly 1 card, plus one small cleanup item — see
below). Nothing here needs you.
- *Needs David:* nothing right now.
- *Other blockers:*
  - The memory-to-external-service integration card is genuinely stuck
    on an upstream limitation: the pinned version of the external memory
    service (Serenity) requires a 3-step network handshake to do
    anything, but this product's own safety rule is "one accounted
    network call per action" — so every capability of that integration
    stays explicitly refused rather than faked, until the upstream
    service changes something. No one on this team can unblock it.
  - A small, self-serviceable cleanup: three packages this session found
    (while implementing their own cards) that the original contract
    freeze missed a couple of small things it was supposed to include —
    a missing pair of identity operations, and two packages independently
    hitting the same "one field marked required but not actually
    declared" mismatch in the frozen catalog. None of this blocks
    anything landing today; it's a same-day-sized cleanup pass this
    session hasn't scheduled yet because it would touch every package's
    generated documentation file at once, and doing that mid-landing-rush
    risked more merge conflicts than it was worth. Will schedule once
    the current batch clears.

## Risks & watch items

- **A real security vulnerability was found and fixed today** (see "In
  progress" above): a symlink-based bypass of a directory-permission
  check, present since the code was written, caught by this remediation
  effort's own review discipline rather than by an external report. The
  fix is implemented, tested 200 times without a miss, and is in the
  landing queue right now — this is not asking for a decision, just
  flagging that it happened, since you'd want to know a real bug like
  this existed even though it's already handled.
- **A load-spike near-miss.** Earlier today, resuming two paused workers
  at once let them both try to run the same expensive full-test-suite
  check simultaneously, which briefly drove the build machine's load
  average past 150 — close to a documented earlier incident (2026-09-18)
  that got as far as exhausting a session's usable capacity at a load of
  about 130. No work was lost and nothing crashed, but the underlying
  cause (a machine-wide build lock that every worker is supposed to
  respect but that nothing actually enforces) is still there. This
  session has since switched to strict one-at-a-time landing to route
  around it, but the actual fix (making the lock mechanism itself
  enforce the rule) is still a todo.
- **A stale lock from a different, unrelated effort.** A file-lock
  record from 12 days ago (2026-09-08, on this repo's execution-plan
  file, from a session on your Mac mini) is still marked active and has
  never been released — it isn't blocking anything in this remediation
  effort specifically, since that plan file isn't in active use here,
  but it's a loose end worth a routine prune the next time you're
  clearing fleet-wide housekeeping.
- **Machine reboot mid-session.** The machine running this session
  restarted partway through today's work (about 16.5 hours of downtime,
  then a 25-minute-old fresh boot when this session picked back up). No
  work was lost — every in-progress package's changes had survived on
  disk — but it's worth knowing this session's continuity depends on a
  machine that isn't guaranteed to stay up.

## Next milestone

The next thing that actually moves the needle, once the three
in-progress cards land (expected within the hour): this session starts
wave 4, the first cards of the actual durable worker loop that the
original audit found missing entirely. That is the point where "package
groundwork is done" starts turning into "the product can do something a
user would recognize as useful."

## Glossary for this report

- **Card**: one numbered unit of remediation work ("P00" through "P49"),
  each scoped to roughly one code package.
- **Wave**: a batch of cards grouped by dependency order — cards in the
  same wave can run in parallel; a later wave's cards depend on an
  earlier wave's cards being done.
- **Milestone (M0-M4)**: a marker of real product capability (e.g. "a
  chief can do useful work"), spanning multiple waves — the thing that
  actually matters, as opposed to a wave, which is just a scheduling
  grouping.
- **This session / lead**: the Claude Code session (name "zatiti" in
  this repo) coordinating the whole effort — claiming cards, dispatching
  background workers to implement them, and landing the result.
- **Worker / lane**: one background AI session doing the actual coding
  for a single card, in its own isolated copy of the repo.
- **Claim**: a lock a session takes on one card (or a shared resource,
  like the build lock) so two workers don't duplicate the same work.
- **Landing / land**: committing a card's finished work to the shared
  main branch after it passes its checks.
- **Tracked-red / expected-red list**: a maintained list of packages
  that are known to fail their tests for an already-understood, already-
  assigned-to-a-card reason — so a new failure that isn't on the list is
  treated as a real problem, while a listed one doesn't block other
  work from landing.
- **Build lock**: a machine-wide lock meant to ensure only one expensive
  full-test-suite run happens at a time on the shared build machine.
