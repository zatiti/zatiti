# Zatiti RFC implementation — sitrep 2026-09-12

## What you need to do

1. **Decide the fate of the open scaffold-import pull request (PR #1, "Import
   Ox Alpha controller scaffold").** This is the third consecutive report
   asking the same question — it has now sat untouched for four days since
   opening on 2026-09-09. It imports a Zatiti controller (the core server
   process that runs the product) that was generated interactively in a
   separate AI console tool ("Ox Alpha"), as a baseline to build on. The
   actual implementation took a different route instead: hand-written
   per-package specifications landing directly on `main` (the shared,
   always-releasable line of code), a decision written down and accepted on
   2026-09-09 in this repo's decision record for that choice
   (`docs/adr/001-directory-scoped-execution.md`), which explicitly states
   the old plan file the scaffold pull request depended on no longer exists.
   GitHub's own merge check still reports the pull request as
   "CONFLICTING" — it cannot be merged without a human resolving overlapping
   changes by hand. Only you can close this: it is your pull request and it
   encodes a strategy choice (keep the console-generated scaffold vs. the
   route actually being run, which has now landed 19 of the 37 planned
   packages). Action: run `gh pr view 1` in this repo, then either resolve
   the conflicts and merge it, or close it. Takes about two minutes. If you
   do nothing, it keeps drifting further from `main` and the eventual
   conflict-resolution cost keeps growing. My recommendation, unchanged for
   the third time: close it as superseded.

Nothing else in this report needs your input right now — see Risks below for
an operational incident (a stuck lock that took 21 hours to notice and
clear) that the integration-lead session is handling directly, not
something that needs a founder decision.

## Verdict

This effort is on track: one more package (artifacts — immutable file and
attachment storage with resumable uploads) landed on `main` since the last
report, verified independently rather than taken on the implementing
agent's word, taking the completed count from 18 of 37 to 19 of 37. The one
thing that matters most right now is not the percentage — it's that a
dispatched work lane went silent while holding a shared lock for 21 hours
(roughly five times the fleet's normal four-hour limit for how long a lock
should be held) before anyone noticed, and only released it after the
integration-lead session messaged it directly. This is the third time in
three days that a dispatched lane has gone silent mid-task; the pattern is
now recurring often enough to be a process problem worth fixing, not just
something to notice and recover from each time it happens.

## Overall completion + trend

**Overall: ~51% (was ~49% at the last report) — one package (artifacts)
landed and independently verified.** The percentage counts packages: the
implementation is frozen into 37 named units of work in
`docs/implementation/packages.json` (the master list this whole plan is
organized around), and "done" means merged to `main` and independently
verified, not merely written. The movement since yesterday is smaller than
the previous day's jump (six packages landed in 24 hours) because most of
today's elapsed time went into a stuck lock, described below, rather than
new implementation work.

## The four states

**Done — 51% (19 of 37 packages).** This counts packages merged to `main`
and verified through the full landing checklist the integration-lead
session runs itself (not taken on the word of whoever wrote the code): the
whole module builds and its tests pass together, a linter finds nothing,
and a "mutation check" — deliberately breaking the package's key safety
rule, confirming a specific test catches the breakage, then restoring the
code exactly — passes. Newly landed since yesterday: artifacts (immutable
file/attachment metadata, resumable chunked uploads, and byte-integrity
checked reads). The mutation check on this landing broke the rule that
stops one organization from reading another organization's artifacts, and
confirmed a specific test would have caught that break (it did — the test
failed exactly as expected before the code was restored). Two design
choices are flagged for whoever later builds the pieces that depend on this
package, not blockers today: retention "pins" (a bookkeeping concept named
in the original design) have no actual operation defined anywhere in the
frozen specification for another package to call, so they are implemented
as internal-only bookkeeping for now; and the "export artifact" operation
can register a durable background job but nothing in the current
specification lets the artifacts package itself drive that job to
completion — some other, not-yet-built package needs to own that.

**In progress — 3% (1 of 37 packages), status now uncertain — see Risks.**
Memory (the product's durable memory store and its history of promoting or
retracting stored facts) has substantial code written — 16 files, about
5,780 lines, including a full test suite — but none of it is committed, and
the session working on it went silent holding a shared build lock for 21
hours. The integration-lead session paged it directly a few minutes before
this report and the lock was released moments later, but there has been no
reply yet confirming whether the underlying module-wide test run actually
passed. Until that reply arrives (or the integration lead re-verifies
directly), this package's true state is: substantial written code, unknown
pass/fail status, not committed.

**Todo — 41% (15 of 37 packages).** Seven packages are the rest of the
current batch ("wave 2"), refined and next in line once the memory
situation resolves: evidence (durable records of what actually happened),
installation (first-time setup, backup, and restore), server (the private
local network interface the product listens on), cli (the command-line
tool), mcp (the adapter that lets AI coding assistants talk to the product
over the Model Context Protocol, a standard way for one program to expose
its features to another), and the GitHub and bounded-web-reading adapters.
Beyond that, eight packages are planned for later batches: the controller
(the core server process), the desktop app, the two command-line entry
points, and four verification/release packages (integration tests,
qualification tests, packaging, and continuous-integration automation).

**Blocked — 5% (2 of 37 packages).** Unchanged from yesterday: the
hosted-AI-model adapter and the Serenity adapter (a separate memory
protocol/service Zatiti reads from) both need a real, working account and
endpoint that the project's dependency-tracking file confirms is not yet
pinned down. Nobody is currently trying to start either package, so this
costs no time today, but you are the only person who can authorize and pay
for a real hosted-model account whenever the queue reaches them.

## Risks & watch items

- **A dispatched lane held a shared lock for 21 hours with no visible
  progress, and nobody noticed until this report was being written.** The
  memory package's session claimed the fleet's shared build lock (a
  mechanism that stops two work lanes from compiling and testing the whole
  product at the same time and stepping on each other) at about 19:35 PDT
  on 2026-09-11 to run its final test pass, then went silent. It held that
  lock continuously until roughly 16:56 PDT on 2026-09-12 — about 21 hours,
  against a fleet norm of 4 hours — which meant the artifacts package,
  whose code was fully written, tested, and verified independently, could
  not run its own final module-wide check or land until the lock freed.
  The integration-lead session only discovered this while gathering data
  for this very report; the lock cleared within a minute of being messaged
  directly, but no confirmation has yet arrived on whether memory's own
  test run passed. This is the third silent stall of a dispatched lane in
  three days (the first two, on the "effects" and "execution" packages, are
  recorded in `docs/roadmap.md`); each time it has been caught by luck or a
  status check rather than by an automatic alert. Worth treating as a
  process gap: nothing currently pages the integration lead when a lock is
  held past its normal limit.
- **The stale lock on the old plan file is now almost five days old.** A
  lock (a "claim," which stops two people from editing the same file at
  once) on a file called `docs/plan.md`, taken out by a Mac mini session on
  2026-09-07, is still marked as held, now at roughly 116 hours against the
  fleet's normal 4-hour limit. That file was superseded by the per-package
  specification approach and no longer exists in the repo, so the lock is
  not actually blocking any real work — flagging again for fleet hygiene
  since it has not been released across three consecutive reports.

## Next milestone

Confirmation of memory's actual build/test status (either its session
replies, or the integration lead re-verifies the code directly) is what
resolves the one open unknown in this report and determines whether the
completed count moves to 20 of 37 quickly or needs rework first. Once that
resolves, the next package in the fixed queue (evidence) is next in line to
dispatch under the two-lane cap.

## Glossary for this report

- **Package**: one self-contained unit of Go code owning one part of the
  product (for example, messaging or scheduling); the whole plan is broken
  into 37 of these.
- **Wave**: one batch of packages worked on together; the current batch is
  "wave 2."
- **Lane**: one independent stream of work — in this project, one package
  being implemented by one dispatched session.
- **Worktree**: an isolated copy of the repository where one package's work
  happens without disturbing `main`.
- **`main`**: the shared, always-releasable line of code every finished
  package eventually merges into.
- **Landing gate**: the integration-lead session's checklist (build, test,
  lint, mutation check) a package must pass before it can merge.
- **Mutation check**: deliberately breaking a package's key safety rule,
  confirming a specific test catches the breakage, then restoring the code
  exactly — proof the test would actually catch a real regression.
- **Integration-lead session**: the Claude Code session (this one, named
  `zatiti`) responsible for merging finished packages into `main` and
  running the landing gate.
- **Claim / lock**: one session locking a shared file or shared resource
  (like the build lock) to itself so another session doesn't collide with
  it.
- **Build lock (`R-build-lease`)**: the specific shared lock that ensures
  only one package's full build-and-test run happens against the whole
  product at a time.
- **MCP (Model Context Protocol)**: the standard protocol that lets an AI
  coding assistant call into another program's features; Zatiti's `mcp`
  package is the adapter that exposes Zatiti this way.
